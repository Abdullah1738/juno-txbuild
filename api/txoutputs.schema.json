{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TxOutputs",
  "type": "array",
  "description": "Explicit Orchard outputs. The signer limit is 200 total outputs including implicit change, so 200 explicit outputs require zero change.",
  "minItems": 1,
  "maxItems": 200,
  "items": {
    "$ref": "#/$defs/TxOutput"
  },
  "$defs": {
    "TxOutput": {
      "type": "object",
      "required": ["to_address", "amount_zat"],
      "properties": {
        "to_address": {
          "type": "string",
          "description": "Unified address (e.g. j1...)"
        },
        "amount_zat": {
          "type": "string",
          "pattern": "^[0-9]+$",
          "description": "Decimal zatoshis (uint64 encoded as string)"
        },
        "memo_hex": {
          "type": "string",
          "description": "Optional memo bytes, hex-encoded (max 512 bytes)"
        }
      },
      "additionalProperties": true
    }
  }
}
